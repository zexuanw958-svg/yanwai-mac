# 更新记录

## 0.3.1 · 2026-09-24

首个公开版本。功能与 0.3.0 相同，只整理成独立仓库。

- 独立开源仓库，只包含 Mac 版，历史从这里开始。
- 根目录加 MIT `LICENSE`（保留上游版权行），打包时一起放进 `言外.app`。
- 重写 `README.md`、`THIRD_PARTY_NOTICES.md`，写清楚和原项目的关系、还没实测的功能。
- `local-model/` 标为实验性：接口和测试替身能跑，没有用真实权重验收。

## 0.3.0 · 2026-09-24

- **Jev 云端**分析方式：Jev 判断对方意图（6 类，完整概率）和建议动作（7 类）→ 可选的 OpenAI 兼容模型写三条候选 → Jev 排序，最高的标「最推荐」。支持博查 Jev、OpenRouter、TypeSafe 官方和自定义接口；回复模型支持 DeepSeek、OpenRouter 和自定义 OpenAI 兼容接口，也可以不用（改用固定模板）。
- **拿不准就说拿不准**：最高意图低于 50%，或领先第二名不到 20 个百分点时，直接标出「这句我也拿不准」，第一条候选换成一句追问。
- **发之前看一眼**：写下你打算发的回复，Jev 给合适度打分、标出风险（会不会让对方更不舒服、有没有接住需求、是否承诺过头、是否敷衍），必要时给一版改法。
- **复制或截图**对话来源：开启后，用微信自带截图（默认 ⌃⌘A）截一张聊天，言外在本机 OCR、分清你和对方，自动分析。截图不保存。
- 检测微信防截屏：「跟随微信」读到空白窗口时提示原因，并给一键切到「复制或截图」的按钮。
- **权限引导卡片**：启动时检查屏幕录制和辅助功能，缺哪个就引导去开，开好自动变 ✓。
- 打包成真正的 `言外.app`：默认停靠屏幕右侧，顶栏可拖，位置会记住；菜单栏图标里有「权限设置…」。
- API Key 用 macOS 钥匙串（Electron `safeStorage`）加密保存，界面拿不到明文。
- 修复：Electron 44 的剪贴板读取改为异步后，「复制」来源和「粘贴」按钮会让主进程崩溃。

## 0.2.x

- 刘海浮窗原型：收起时贴合刘海，展开可贴顶或停在右侧，`⌘⇧J` 呼出。
- 示例体验（职场 / 日常 / 恋爱三组模拟数据）、意图概率条、三条候选与「最推荐」。
- 对话来源：手动粘贴 / 跟随复制 / 跟随微信（按窗口读取，本机 Vision OCR）。
- 「填入」：只把候选追加进当前输入框，绝不自动发送；权限不足时退回复制。
- 本地模型入口（`127.0.0.1:7147`）。
